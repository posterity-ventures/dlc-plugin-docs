# Prompt — artifacts-lifecycle.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the lifecycle of AI-DLC artifacts inside a feature directory. Title banner at the top spelled exactly: "AI-DLC Artifacts in Feature Directory". Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for the artifact boxes and warm amber (#fbbf24) for the "permanent vs transient" distinction callout. Sans-serif typography, crisp labels. Central root box labeled exactly: "${DLC_ARTIFACT_ROOT:-.dlc}/slug/". Radiating from it, rounded rectangles for each artifact labeled: "state.md", "requirements.prd.md", "designs/tech-design.md", "plans/epic-plan.md", "analysis_output/", "telemetry.jsonl", "escalation-context.md". A amber dashed boundary separates permanent artifacts (requirements.prd.md, tech-design.md, analysis_output) from transient ones (state.md, telemetry.jsonl, plans, escalation-context). A small legend at the bottom-right: "solid = permanent, dashed = transient (cleaned by finalize-sdlc)". Minimal line icons — a document for each file, a folder for each directory. Clear hierarchy, no photo-realistic elements.

# Prompt — git-flows-comparison.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style comparing the four branching-model presets supported by this plugin. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for branch lines and commit dots, warm amber (#fbbf24) for environment badges and the "default" marker. Sans-serif typography, crisp labels.

A two-by-two grid with four quadrants, each showing a miniature branching-model diagram. Each quadrant has a title label at the top.

Top-left quadrant title labeled exactly "GitLab Flow (default)". Shows three horizontal branch lines stacked vertically, labeled exactly "main", "staging", "prod" from top to bottom. Blue arrows show a feature branch merging into main, then main merging down into staging, then staging merging down into prod. Small amber badges at the right end of each lane labeled exactly "dev env", "stg env", "prod env".

Top-right quadrant title labeled exactly "GitHub Flow". Shows a single horizontal branch line labeled exactly "main". Two short feature branches branch off and merge back into main. One amber badge at the right end labeled exactly "deploy per merge".

Bottom-left quadrant title labeled exactly "Gitflow". Shows three horizontal branch lines stacked vertically, labeled exactly "main", "release", "develop" from top to bottom. Feature branches merge into develop; a release branch splits off from develop and eventually merges into both main and back into develop. Small amber tag labeled exactly "tagged releases" near the main line.

Bottom-right quadrant title labeled exactly "Trunk-based". Shows a single horizontal branch line labeled exactly "main". Very short feature branches merge back within the same day. Amber tag labeled exactly "feature flags" hovering near the main line.

Minimal branching-tree line icons, consistent stroke width. Each quadrant is clearly separated by a thin amber divider. Overall composition is balanced and scannable at a glance. No photo-realistic elements.

# Prompt — gitflow.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the Gitflow branching model. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for long-lived branch lines, warm amber (#fbbf24) for release/hotfix/environment callouts. Sans-serif typography, crisp labels. Two horizontal parallel branch lines left-to-right, top to bottom: "main" on top and "develop" below it. Feature branches labeled exactly "feat/slug" and "fix/slug" split off from "develop" and merge back into "develop". A "release/x.y" branch splits off from "develop", goes to the right, and merges into both "main" (top) and back into "develop" with two labeled arrows. An amber "hotfix/slug" branch splits off from "main", goes briefly right, and merges back into both "main" and "develop". To the right of "main", an amber pill reads exactly "prod env". To the right of "develop", an amber pill reads exactly "dev env". Small callout at the top-left reads "Long-lived develop + main. Release branches for stabilization." Minimal line icons — git branch symbols, small circle markers at each merge point. Clear hierarchy, no photo-realistic elements, consistent stroke width.

# Prompt — github-flow.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the GitHub Flow branching model. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for the main branch line and feature branches, warm amber (#fbbf24) for deploy labels and callouts. Sans-serif typography, crisp labels. One horizontal branch line labeled exactly "main" running left-to-right across the full width. Above "main", show three short feature branches splitting off and merging back in at different points, labeled exactly "feat/slug", "fix/slug", "chore/slug". Each merge back into main is labeled "PR". To the right of main, a small amber pill labeled exactly "prod (continuous deploy)" with a rocket icon indicating that every merge to main deploys to production. Small callout at the top-left reads "Single long-lived branch. Deploy on merge." Minimal line icons — a git branch symbol for main, small circle markers at each merge point. No staging or release branches. Clear hierarchy, no photo-realistic elements, consistent stroke width.

# Prompt — gitlab-flow.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the GitLab Flow branching model used in this repository. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for branch lines and warm amber (#fbbf24) for environment labels. Sans-serif typography, crisp labels. Three horizontal parallel branch lines left-to-right, top to bottom: "main", "staging", "prod". Above "main", show short feature branches splitting off and merging back in (one labeled "feat/slug", one labeled "fix/slug", one labeled "chore/slug"). Between "main" and "staging" draw a labeled arrow "merge commit only". Between "staging" and "prod" draw a labeled arrow "merge commit only". To the right of each main branch line, amber boxes label the environment: "dev env" next to main, "stg env" next to staging, "prod env" next to prod. Small callout at the top-left reads "Promotion PRs must use merge commits, never squash." Minimal line icons — a git branch symbol for each line. Clear hierarchy, no photo-realistic elements.

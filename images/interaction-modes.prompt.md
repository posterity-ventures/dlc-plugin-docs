# Prompt — interaction-modes.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style comparing the three interaction modes that control checkpoint behavior across the orchestrated SDLC. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for the workflow lanes and warm amber (#fbbf24) for checkpoint markers and mode labels. Sans-serif typography, crisp labels.

Three horizontal lanes stacked vertically, each representing one interaction mode. Each lane shows the same sequence of SDLC phases as a row of small blue boxes labeled exactly in order: "Requirements", "Design", "Implementation", "PR", "Stabilization", "Merge".

Left side mode labels for each lane (top to bottom), each inside an amber pill: "interactive", "confident", "autopilot".

In the top lane (interactive) place small amber diamond checkpoint markers labeled exactly "Checkpoint" above every transition between phases — six total diamond markers.

In the middle lane (confident) place amber diamond checkpoint markers labeled exactly "Checkpoint" above only three transitions: between "Requirements" and "Design", between "PR" and "Stabilization", and before "Merge".

In the bottom lane (autopilot) place zero checkpoint markers. Instead, below the lane add a single amber tag labeled exactly "Decisions Log" connected by a dashed amber arrow, representing the audit trail that replaces interactive pauses.

On the far right of each lane place a small caption, amber for autopilot and soft blue for the others. Top lane caption reads exactly "pauses at every phase". Middle lane caption reads exactly "pauses at major gates only". Bottom lane caption reads exactly "never pauses, logs every decision".

Minimal line icons, consistent stroke width, clear visual comparison across the three lanes. No photo-realistic elements.

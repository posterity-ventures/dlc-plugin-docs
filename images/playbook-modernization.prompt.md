# Prompt — playbook-modernization.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the modernization playbook with incremental shipment strategy. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for strategy boxes and warm amber (#fbbf24) for the rollback/canary callouts. Sans-serif typography, crisp labels. A top box labeled exactly "Strategy doc + epic issue". Below it, three horizontal parallel lanes representing the parallel-change pattern labeled exactly left to right: "1. Expand (add new alongside old)", "2. Migrate (move callers one PR at a time)", "3. Contract (delete old)". Each lane contains three small PR boxes showing incremental work. Below the lanes, a box labeled exactly "Decommission PR". Amber callouts on the right read "Rollback plan" and "Canary deploy". Minimal line icons — a document for strategy, a fork symbol for expand, arrows for migrate, a trash for contract, a checkmark for decommission. Clear hierarchy, consistent stroke width, no photo-realistic elements.

# Prompt — playbook-troubleshooting.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the troubleshooting playbook as a decision tree for failure modes. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for category boxes and warm amber (#fbbf24) for the diagnostic commands callout. Sans-serif typography, crisp labels. Central diamond labeled exactly "What broke?". Five branches radiating outward to category boxes labeled exactly: "CI & stabilization", "Worktrees", "Hooks", "Artifacts & state", "MCP servers". Each category has a small icon. Below the central diamond, a separate amber box labeled exactly "Diagnostic commands" with a small terminal icon — representing the section that suggests running a few shell one-liners. Off to the side, a smaller box labeled "Hotfix path (incident)" in amber outlined red, representing the separate incident-response branch. Minimal line icons — a pipeline for CI, a folder tree for worktrees, a lightning bolt for hooks, a document stack for artifacts, a plug for MCP. Clear hierarchy, no photo-realistic elements.

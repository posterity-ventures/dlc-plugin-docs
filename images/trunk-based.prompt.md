# Prompt — trunk-based.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the Trunk-Based Development branching model. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for the trunk line and short-lived branches, warm amber (#fbbf24) for feature-flag callouts and the deploy label. Sans-serif typography, crisp labels. One horizontal branch line labeled exactly "main" running left-to-right across the full width — visually thicker than the feature branches to emphasize it is the trunk. Above "main", show four very short feature branches (much shorter than in the GitHub Flow diagram) splitting off and merging back in quickly, labeled exactly "feat/slug", "fix/slug", "chore/slug", "refactor/slug". Each merge back is labeled "PR". To the right of main, an amber pill labeled exactly "prod (deploy on green)" with a rocket icon. Three small amber flag icons hover above the main line at different points with a callout reading exactly "feature flags gate unfinished work". Small callout at the top-left reads "Short-lived branches. Trunk always releasable." Minimal line icons — a git branch symbol, flag icons for the feature flags, small circle markers at each merge point. Clear hierarchy, no photo-realistic elements, consistent stroke width.

# Prompt — trunk-based.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing the Trunk-Based Development branching model. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for the trunk line and short-lived branches, warm amber (#fbbf24) for feature-flag callouts and the deploy label. Sans-serif typography, crisp labels. Diagram title across the top reads exactly "Trunk-Based Development" in large bold sans-serif. One horizontal branch line labeled exactly "main" running left-to-right across the full width — visually thicker than the feature branches to emphasize it is the trunk. Above "main", show four very short feature branches (much shorter than in the GitHub Flow diagram) splitting off and merging back in quickly, labeled exactly "feat/slug", "fix/slug", "chore/slug", "refactor/slug". Each merge back is labeled "PR". To the right of main, a single amber pill labeled exactly "prod (deploy on green)" with a rocket icon. Below the trunk, a single amber callout box (appearing exactly once, not repeated) contains one amber flag icon and the text "feature flags gate unfinished work" — this callout should NOT be duplicated. Small annotation at the top-left reads "Short-lived branches. Trunk always releasable." Minimal line icons — a git branch symbol, one flag icon inside the single callout box, small circle markers at each merge point. Clear hierarchy, no photo-realistic elements, consistent stroke width. Do not include any other repeated labels or duplicate callouts anywhere in the image.

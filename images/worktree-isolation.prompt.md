# Prompt — worktree-isolation.png

**Include** [_style.md](_style.md) preamble before the prompt body.

**Parameters**: `aspectRatio: 16:9`, `imageSize: 2K`

## Prompt body

A clean, modern technical diagram in flat vector illustration style showing git worktree isolation across multiple parallel SDLC sessions. Dark-mode-friendly deep slate background (#1e293b). Soft blue (#60a5fa) for worktree boxes and warm amber (#fbbf24) for the shared .git/ callout. Sans-serif typography, crisp labels. A central single shared box labeled exactly: ".git/" (shared object store). Radiating outward to three separate worktree boxes labeled exactly: ".worktrees/feat/auth-refresh", ".worktrees/fix/payment-retry", ".worktrees/chore/docs-update". Each worktree has its own branch label (feat/auth-refresh, fix/payment-retry, chore/docs-update) and its own file-tree icon indicating an independent checkout. Amber arrows connect each worktree back to the shared .git/. Small caption at the bottom: "each worktree = independent checkout, shared history". Minimal line icons — folder symbols for each worktree, a small database icon for .git/. Clear hierarchy, no photo-realistic elements.
